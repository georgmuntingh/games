// Hand-curated facts the map image cannot express, merged over the
// generated ROOMS data by overworld.js (shotHearts, entityProps). Empty
// for now — the map-extracted rooms use only what the classifier emits.
export const OVERRIDES = {};
